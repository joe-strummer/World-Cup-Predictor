import { RoundHeading } from '../'

export function SemiFinalsContainer() {
  return (
    <section className="container">
      <RoundHeading
        heading="Semi Finals"
        subheading="Pick the winner of each match"
      />
    </section>
  )
}
