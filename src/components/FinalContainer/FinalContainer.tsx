import { RoundHeading } from '../'

export function FinalContainer() {
  return (
    <section className="container">
      <RoundHeading
        heading="Final"
        subheading="Pick the World Cup winner"
      />
    </section>
  )
}
